# homebrew-tap

Personal Homebrew tap for [AgentDeck](https://github.com/kitdine/agent-deck).

```bash
brew install kitdine/tap/agentdeck
agentdeck version
```

The formula installs the release binary only. Formula version and checksums
are updated manually per release.
